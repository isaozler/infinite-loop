{
  "name": "infinite-loop",
  "source": "src/index.html",
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "scripts": {
    "start": "parcel",
    "build": "parcel build",
    "prebuild": "shx rm -rf dist/*",
    "build-dev": "parcel build ./src/index.html --no-optimize --public-url ./infinite-loop",
    "push-gh-pages": "push-dir --dir=dist --branch=main --cleanup --verbose"
  },
  "devDependencies": {
    "@parcel/config-default": "^2.4.1",
    "@parcel/transformer-typescript-tsc": "^2.4.1",
    "@types/d3": "^7.1.0",
    "parcel": "latest",
    "typescript": "^4.6.3"
  },
  "dependencies": {
    "d3": "^7.4.0",
    "shx": "^0.3.4"
  }
}
